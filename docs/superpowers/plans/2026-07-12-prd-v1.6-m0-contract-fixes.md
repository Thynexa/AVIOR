# PRD v1.6 M0 Contract Fixes Implementation Plan

> **状态：✅ 已完成（2026-07-12）** — 经 PR #17 合并入 `main`（含评审后的 R-FDA 依据分层修订）。本文件保留为工作档案。

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the A-level contract gaps and B-level schema reservations identified in `docs/product-design-review.md` by revising PRD to v1.6 and synchronizing `examples/minimal-project/` before the M0 schema freeze.

**Architecture:** Pure documentation/data change. Three units: (1) PRD.md revision, (2) example project sync (including MANIFEST regeneration), (3) root README housekeeping. Everything lands on branch `claude/product-design-review-w2q5rj` (which already carries the review doc) and becomes PR #1.

**Tech Stack:** Markdown, YAML, JSON, `sha256sum`.

## Global Constraints

- PRD is the controlled baseline: every change gets an entry in §13 修订记录 as **v1.6**, citing `docs/product-design-review.md` item IDs (A1–A6, B1–B5, C1).
- Do not violate the three axioms (§2): no server, evidence stays files, no signing.
- Example bundle files must stay internally consistent: `snapshot/` copies == `validation/` sources; `MANIFEST.sha256` must be regenerated (sorted by path, format `<sha256>  <relpath>`, excludes itself).
- All timestamps in examples stay illustrative but format-correct: `YYYY-MM-DDTHH:MM:SSZ`.

---

### Task 1: PRD.md v1.6 revision

**Files:**
- Modify: `docs/PRD.md`

**Edit list (review item → PRD location → change):**

- [ ] **A1 → §5.0**: Add row **FR-X-8「规范化序列化」** (P0): all generated artifacts are UTF-8 without BOM, LF line endings on every platform; numbers in decimal notation (never scientific), round-half-even to max 4 fractional digits, trailing zeros trimmed but at least one fractional digit kept for scores/weights (`1` → `1.0`, `0.6100000000000001` → `0.61`); timestamps UTC ISO-8601 seconds (`YYYY-MM-DDTHH:MM:SSZ`); YAML generated in block style, 2-space indent, keys in schema-defined order, strings quoted only when required; CSV fields quoted only when containing ASCII comma, double quote, or newline. Note: FR-X-7 + FR-X-8 together are the preconditions of the byte-identical ACs (NFR-1).
- [ ] **A2 → §6.5**: Define `version_managed` as the fixed value of the `decision` column for `transitive` rows (row-level status, separate namespace from the FR-REVIEW-2 decision enum, which remains include/include_with_tests/exclude). Update the sentence about empty columns accordingly.
- [ ] **A3 → FR-BUNDLE-5**: Extend environment fingerprint: locale (at least `LC_COLLATE`), BLAS/LAPACK implementation & version (via `sessionInfo()`; record `"unknown"` if undetectable, never omit the key), container image digest when detectable (else `null`), and full `sessionInfo()` text as bundle file `session-info.txt`; add `session-info.txt` to FR-BUNDLE-2 content list.
- [ ] **A4 → FR-TEST-2**: test-results.yml additionally records the `renv.lock` SHA-256 plus R version/platform at run time; a lockfile-hash mismatch versus the current inventory baseline makes results 过期 (same staleness semantics as version mismatch). Mirror the rule in FR-CHECK-2.
- [ ] **A5 → FR-BUNDLE-3**: Add fixed report section「范围与边界声明」 between 方法论引用 and 范围与分类: states what the bundle does NOT cover (computing-environment IQ/OQ/PQ — sponsor IT responsibility under its QMS; analysis-code QC/double programming; Part 11 controls — host-system responsibility), with responsibility attribution; base/recommended default exemption must cite R Foundation《R: Regulatory Compliance and Validation Issues》(R-FDA.pdf) as its source.
- [ ] **A6 → FR-CHECK-2 + FR-REVIEW-3**: New red rule `excluded_but_present`: a package with decision `exclude` still present in the lockfile fails check; fix suggestion「从项目移除该依赖，或修订决策并说明」.
- [ ] **B1 → §7.2 + §6.2**: Metric registry entries gain `cost: metadata | network | execution`; `execution`-cost metrics (e.g. `covr_coverage`) are NOT run in batch assess by default — explicit opt-in via `--deep` or per-package config; §6.2 comment on `covr_coverage` notes it is execution-tier/opt-in. Fix metric id `bugs_status` → `last_30_bugs_status` anywhere it appears; note ids must exist in the adapter registry (existing rule).
- [ ] **B2 → FR-X-5 + FR-ASSESS-3**: Cache entries record `na_metrics`; a cache hit whose `na_metrics` is non-empty while network is now available is treated as an improvable hit → re-scored by default (configurable). scores.yml records per-package `na_metrics` (omit when empty) in addition to the top-level aggregate; report shows effective (reweighted) weights per package.
- [ ] **B3 → FR-BUNDLE-1 + NFR**: zip redefined as transport artifact: generated only with `--zip`, gitignored by default (directory + manifest are the archival form; zip reproducible via SOURCE_DATE_EPOCH). Add one-line retention guidance (git history is the archive; working tree may prune old bundles).
- [ ] **B4 → §6.3**: Decision schema gains reserved fields `assessment_type: initial` (enum initial|delta; V1 always initial) and `supersedes: null` (V2: reference to superseded decision for delta assessments). Show them in the example YAML.
- [ ] **B5 → NFR-5**: Split into NFR-5a (pipeline dogfooding: avior runs on AVIOR's own dev dependencies in CI, producing a bundle) and NFR-5b (self risk profile: CI job scores the avior package itself with the default engine/policy; must land in the low tier).
- [ ] **C1 → §10-5**: Add measurement scope note: metric counted within design-partner projects during pilot + open-source adoption proxies; explicitly no telemetry (NFR-4).
- [ ] **§13**: Add v1.6 revision row (date 2026-07-12) referencing `docs/product-design-review.md`.
- [ ] Bump header 文档版本 to v1.6.

**Verify:** re-read changed sections; grep PRD for `bugs_status`(0 hits), `FR-X-8`(≥2 hits: §5.0 + revision log).

- [ ] **Commit**: `docs: PRD v1.6 — M0 contract fixes from product design review (A1-A6, B1-B5, C1)`

### Task 2: Example project sync

**Files:**
- Modify: `examples/minimal-project/validation/avior.yml` (metric id fix)
- Modify: `examples/minimal-project/validation/scores.yml` (metric id fix)
- Modify: `examples/minimal-project/validation/test-results.yml` (add `environment: {lockfile_sha256, r_version, platform}` — A4)
- Modify: `examples/minimal-project/validation/decisions/*.yml` (4 files: add `assessment_type: initial` + `supersedes: null` — B4)
- Modify: bundle `environment.json` (add `locale`, `blas`, `lapack`, `container` — A3)
- Create: bundle `session-info.txt` (illustrative sessionInfo output — A3)
- Modify: bundle `report.html` (insert「范围与边界声明」section + R-FDA.pdf citation — A5)
- Modify: bundle `snapshot/` copies of avior.yml/scores.yml/test-results.yml/decisions (keep == sources)
- Modify: bundle `MANIFEST.sha256` (regenerate)
- Modify: `examples/minimal-project/README.md` (note: generated YAML is comment-free block style; example files carry pedagogical comments, structure/fields are contract-exact)

**Steps:**

- [ ] Verify recorded lockfile hash is real: `sha256sum examples/minimal-project/renv.lock` must equal `ffb8b2…` in inventory.yml/environment.json; if not, fix the recorded values.
- [ ] Apply the file edits above (keep `version_managed` in traceability.csv — now legal per §6.5).
- [ ] Regenerate manifest:
```bash
cd examples/minimal-project/validation/evidence/bundle-20260708T120000Z
find . -type f ! -name MANIFEST.sha256 | sed 's|^\./||' | LC_ALL=C sort | xargs sha256sum > MANIFEST.sha256
```
- [ ] Verify: `sha256sum -c MANIFEST.sha256` passes; `diff` each snapshot copy against its validation/ source.
- [ ] **Commit**: `docs: sync minimal example with PRD v1.6 contracts`

### Task 3: Root README housekeeping (C3)

**Files:**
- Modify: `README.md`

- [ ] Replace both PRD version badges/texts (`v1.2`) with unversioned「development baseline」wording so it can't drift again; update repo-layout tree: `r-package-validation-strategy.docx` → `.md`, add `product-design-review.md`.
- [ ] **Commit**: `docs: fix stale PRD version refs and doc links in README`

### Task 4: Subagent review gate + PR

- [ ] Dispatch a reviewer subagent over `git diff main...HEAD` with the PRD-consistency checklist (every review item A1-A6/B1-B5/C1 has a corresponding edit; no contradiction with §2 axioms; example internally consistent; manifest verifies). Fix CONFIRMED findings.
- [ ] Push branch; create PR #1 (base `main`, head `claude/product-design-review-w2q5rj`).
